services:
  rstudio:
    image: rocker/tidyverse:latest
    container_name: rstudio
    ports:
      - "8787:8787"
    environment:
      - PASSWORD=${RSTUDIO_PASSWORD}
    volumes:
      - ./:/home/rstudio/work
